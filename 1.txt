git config --global user.name "reshammeghnani"
git config --global user.email "reshammeghnani4321@gmail.com"
git clone https://github.com/reshammeghnani/testingrepo.git
cd testingrepo
git status
echo "Hello Git git" > file.txt
git add .
git commit -m "Initial commit"
git push origin main
git pull origin main

hehehe



git config --global user.name "reshammeghnani"
git config --global user.email "reshammeghnani4321@gmail.com"

git clone https://github.com/reshammeghnani/testingrepo.git
cd testingrepo

git status

echo "Hello Git git" > file.txt

git add .
git commit -m "Initial commit"

git push origin main

git pull origin main

git checkout -b feature-branch

echo "Feature branch content" >> file.txt

git add .
git commit -m "Added changes in feature branch"

git push origin feature-branch

git checkout main

git merge feature-branch

git push origin main
